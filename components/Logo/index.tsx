import React from 'react';
import styles from './Logo.module.scss';

const Logo = () => {
    return (
        <div>
            <h2 className={styles.logoText}>Digital Bridge</h2>
        </div>
    );
};

export default Logo;
